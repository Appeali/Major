#include <stdio.h>
main()
{
  FILE *rf;
  int mm;
  int w[i];
  rf=fopen("C:\\mhm.txt","r");
  fscanf(rf,"%d",&mm);
  printf("%d\n",mm);
  for(int i=1;i<5;i++)
  {
    fscanf(rf,"%d",&w[i]);
    printf("%d\n",w[i]);
  }
}
